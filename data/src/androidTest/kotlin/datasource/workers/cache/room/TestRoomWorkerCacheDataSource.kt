package datasource.workers.cache.room

import kotlinx.coroutines.runBlocking
import org.junit.Assert
import org.junit.Test

class TestRoomWorkerCacheDataSource {

    /*@Test
    suspend fun `test get worker`() = runBlocking {
        val expected = roomCacheDataSource.get()

        Assert.assertTrue(expected.workers.size == 2)
    }

    @Test
    suspend fun `test get empty worker`() = runBlocking {
        val expected = roomCacheDataSource.get()

        Assert.assertTrue(expected.workers.size == 2)
    }*/
}